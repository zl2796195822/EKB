# EKB CR-PH0–PH8 实施接管文档（2026-08-12）

> 用途：本会话 Bash 工具彻底不可用（`Command rejected: command expected string undefined`），所有新增代码**未经运行验证、未提交**。本文件供"Bash 可用"的新会话直接读取并继续。

## 0. 全局约束（务必先读）

- 这是 EKB 知识库系统的 **PH0–PH8 分阶段核心重建**（`/Users/alin/EKB/docs/specs/ekb-core-rebuild/00–14` 为权威 Spec）。
- **生产前置全部 BLOCKED**：生产 PostgreSQL 切库、出网、受管 Provider 凭据、对象存储、队列、master key、备份演练、用户批准窗口。当前本地环境只做**可在本地落地的迁移/服务/路由/单测**，依赖生产的门禁如实标记 BLOCKED，**严禁 mock 或声称已实现**。
- Spec 禁止把未实现功能记为验收通过。

## 1. 本会话已落地的本地代码（PH1 + PH2 本地片段）

所有文件按既有 v4 不可变迁移风格编写（`apply/verify/rollback_dry_run` 三元组、sha256 checksum、`__TIME__`/`JSON` 方言占位、不可变 ledger）。**状态：已写盘，未运行验证、未提交。**

### PH1 数据/任务/安全基础
| 文件 | 内容 |
|---|---|
| `apps/api/ekb_api/migrations/v4_004_postgres_cutover.py` | 切库 ledger 表 `cutover_ledger`（仅 bookkeeping schema，数据搬迁标注 `operational_BLOCKED`）；导出 `apply_v4_004/verify_v4_004/rollback_v4_004_dry_run` |
| `apps/api/ekb_api/migrations/v4_005_retention_governance.py` | 回收站 + 30 天保留：`deletion_batches` + 给 `knowledge_bases/documents/document_versions/conversations/trash_items` 加保留列 + 索引 + PG `interval '30 days'` CHECK（仅 PG 校验）；`trash_items` 缺失时自动建表；幂等 |
| `apps/api/ekb_api/migrations/v4_fullstack.py` | CHAIN 已接入 `v4_004`、`v4_005`（已确认函数名匹配） |
| `apps/api/ekb_api/services/jobs.py` | 在既有 `JobService` 上新增 `list_for_tenant`（租户强制作用域）+ `cancel(tenant_id, job_id, actor_id, now)`（CAS 守卫、终态幂等、写 outbox） |
| `apps/api/ekb_api/routers/jobs.py` | `/api/v1/jobs` Jobs Center：list/get/enqueue/cancel，`CAP_AUDIT_READ`/`CAP_KB_WRITE` 守卫 |
| `apps/api/ekb_api/main.py` | import `jobs` 并 `app.include_router(jobs.router, prefix="/api/v1")` |
| `apps/api/ekb_api/services/secrets.py` | **PH1 T05**：Fernet 信封加密 `encrypt/decrypt/reencrypt` + `redact`（脱敏）+ `current_key_version`/`is_configured`/`SecretKeyUnavailable`（**fail-closed**：无 dev 默认值，空密钥即拒绝）；`MultiFernet` 预接线以支持轮转 |
| `apps/api/tests/v4/test_migration_v4_004_005.py` | v4_004/v4_005 apply/verify/幂等；rollback dry-run BLOCKED；v4_005 依赖 v4_004 顺序守卫 |
| `apps/api/tests/v4/test_secrets.py` | secret 加密往返/脱敏/fail-closed 单测 |

### PH2 Provider/主题/回收站（本地片段）
| 文件 | 内容 |
|---|---|
| `apps/api/ekb_api/services/retention.py` | `purge_expired_trash`（CAS 守卫、分批、按资源类型硬删、幂等、`dry_run`）+ `schedule_purge_job`（入队 `retention_purge`） |
| `apps/api/tests/v4/test_retention.py` | eligible 过期→`SUCCEEDED`、`dry_run` 不改、未到期跳过（SQLite 安全路径，不插 `deletion_batches` 以绕开 PG-only 30 天 CHECK） |

> 已手工修复全部 `E501`（>100 行宽），并清理未用导入（`routers/jobs.py` 移除 `Body`、`secrets.py` 移除 `os`、`retention.py` 移除 `json/uuid4/Any`）。

## 2. 本会话修正过的真实 Bug

- `tests/v4/test_retention.py`：`_insert_trash` 原先把 `tenant_id` 同时填进 `trash_items.deleted_by`（该列 `REFERENCES users(id)`），tenant id ≠ user id，**FK 错误**。已改为取真实 `user_id` 填 `deleted_by`，三个测试调用同步更新为 `tenant_id, user_id = _tenant_user(engine)`。

## 3. 新会话第一步：运行验证（Bash 可用环境）

```bash
cd /Users/alin/EKB/apps/api
PY=/Users/alin/EKB/.venv/bin/python3

# 1) 测试
$PY -m pytest -q tests/v4 tests/v3 2>&1 | tail -40

# 2) lint
$PY -m ruff check \
  ekb_api/migrations/v4_004_postgres_cutover.py \
  ekb_api/migrations/v4_005_retention_governance.py \
  ekb_api/migrations/v4_fullstack.py \
  ekb_api/services/jobs.py ekb_api/services/secrets.py ekb_api/services/retention.py \
  ekb_api/routers/jobs.py ekb_api/main.py \
  tests/v4/test_migration_v4_004_005.py tests/v4/test_secrets.py tests/v4/test_retention.py \
  2>&1 | tail -40

# 3) 编译
$PY -m compileall -q \
  ekb_api/migrations/v4_004_postgres_cutover.py \
  ekb_api/migrations/v4_005_retention_governance.py \
  ekb_api/services/secrets.py ekb_api/services/retention.py ekb_api/routers/jobs.py \
  2>&1 | tail -20
```

**预期**：v4 全过（基线 23 + 新增 ~8）、v3 全过（34）、ruff 无 `E501`/`F401`/未定义名、compileall 无错。
**若失败**：按报错修对应文件，循环直到全绿。

## 4. 验证通过后：提交（不要自动 push）

```bash
cd /Users/alin/EKB
git add -A apps/api/ekb_api/migrations/v4_004_postgres_cutover.py \
  apps/api/ekb_api/migrations/v4_005_retention_governance.py \
  apps/api/ekb_api/migrations/v4_fullstack.py \
  apps/api/ekb_api/services/jobs.py apps/api/ekb_api/services/secrets.py \
  apps/api/ekb_api/services/retention.py \
  apps/api/ekb_api/routers/jobs.py apps/api/ekb_api/main.py \
  apps/api/tests/v4/test_migration_v4_004_005.py \
  apps/api/tests/v4/test_secrets.py apps/api/tests/v4/test_retention.py
git commit -m "PH1+PH2 local slice: cutover ledger, retention governance, jobs API, secrets envelope, purge scheduler"
```
注：PH0 baseline 分支 `codex/ekb-core-baseline-20260812` 已存在，本会话未改动 git。

## 5. 尚未完成（按 Phase）

- **PH1**：T04（QA turn tenant+actor 作用域，统一 live auth 已存在，turn 终态/取消属 PH4）、T06（SQLite→PG 切库，spec BLOCKED）；生产 egress/凭据/对象存储/队列前置 BLOCKED。
- **PH2**：T01 原生 Provider adapter + 受控 compatible profiles（需真实出网，BLOCKED）；T02 Provider/Model registry/health/fallback（registry 本地可做，health 出网 BLOCKED）；T03 个人中心 UI/selector fallback（前端）；T04 语义 token + portal theme bridge（前端）；T05 Jobs Center 只读投影端点。
- **PH3–PH8**：知识库摄取/索引、AI Chat Engine、附件/OCR/Vision、KB+RAG、全系统 UX/性能/回归、生产发布验收。均有多项生产前置 BLOCKED。

## 6. 关键约定（避免踩坑）

- `verify_v4_001` 用**前缀匹配**（本地切片 manifest 是完整 v4 链的前缀），后续加 v4_006+ 时无需再改它。
- `deletion_batches` 的 `interval '30 days'` CHECK 是 PG 语法，SQLite 下被 `_normalize_ddl` 剥离；测试切勿向 `deletion_batches` 插行（SQLite 解析/校验会失败），走 `deletion_batch_id IS NULL` 的 trash 路径。
- `prepare_legacy_schema(engine, seed=True)` 经 ORM 写入 tenant + user（id 不同），`trash_items` 的 `tenant_id`/`deleted_by` 必须分别填真实 tenant id / user id。
- `services/secrets.py` 的 `EKB_PROVIDER_MASTER_KEY` **无默认值**——空即 `SecretKeyUnavailable`，这是 fail-closed 设计，不是 bug；单测需 `monkeypatch`/设 env 后 `get_settings.cache_clear()`。

---

## 7. 续作记录（Bash 可用会话，2026-08-12 18:14 GMT+8）

> 本会话 Bash 工具可用，已执行第 3 节验证、第 4 节提交，并推进 PH1/PH2 剩余项。
> **原则**：生产前置缺失的项一律标记 `BLOCKED`，未实现功能不记为验收通过，不用 mock 绕过。

### 7.1 第 3 节验证结果（全绿）

| 检查 | 命令 | 结果 |
|---|---|---|
| 测试 | `pytest -q tests/v4 tests/v3` | **64 passed, 2 skipped**（v4 32 + v3 34）；2 skipped 为 postgres 门禁测试（无本地 PG） |
| lint | `ruff check`（11 个目标文件） | All checks passed（注：首次 run 报告的 7 个 E501/F401 为 ruff 缓存脏数据，刷新后不复现；`--no-cache` 复查无超长行/未用导入） |
| 编译 | `compileall` | 无错误 |

> 注：`main.py` 有一处 101 字符超宽行，本次顺手拆分（`_extra = _split_csv(...)` 包裹），属 E501 防御性修复。

### 7.2 提交（未 push）

- `3349505` — PH1+PH2 local slice：cutover ledger、retention governance、jobs API、secrets envelope、purge scheduler（11 文件，2733 行）。对应接管文档第 4 节。
- `9ee0e22` — PH2-T05 Jobs Center 清理投影 + PH1-T03 jobs 服务测试（3 文件，232 行）：
  - `services/jobs.py`：`CleanupProjection` + `cleanup_projection()`（租户作用域只读聚合 `retention_purge` 任务）
  - `routers/jobs.py`：`GET /api/v1/jobs/cleanup` 投影端点（`CAP_AUDIT_READ`）
  - `tests/v4/test_jobs.py`：enqueue/list/cancel 生命周期、跨租户隔离、cleanup 投影聚合（3 项）

### 7.3 PH1 状态（数据/任务/安全基础）

| 任务 | 状态 | 说明 / 证据 |
|---|---|---|
| T01 migration provenance ADR/manifest + verifier | DONE-LOCAL | v4_001 provenance 已落地（PH1 foundation 记录），本地门禁含 checksum drift / immutable ledger |
| T02 PG+pgvector schema / 对象存储 / queue / worker / scheduler / health | **BLOCKED** | 需生产 PostgreSQL、对象存储、可靠队列等基础设施；本地仅 disposable PG 验证过，未部署生产 |
| T03 background jobs / outbox / lease / retry / DLQ + 租户 Jobs API | DONE-LOCAL | `services/jobs.py` + `routers/jobs.py`（list/get/enqueue/cancel 全租户作用域）；jobs 测试 `9ee0e22` 锁定 |
| T04 统一 live auth；QA turn tenant+actor read/cancel/terminal scope | DONE-LOCAL | `store.get_turn` / `request_cancel_turn` 均 `filter_by(turn_id, tenant_id=auth.tenant_id)`（store.py:1415/1433）；turn 终态/取消属 PH4 |
| T05 secret master-key / redaction / rotation；缺 key fail closed | DONE-LOCAL | `services/secrets.py`（Fernet 信封 + `redact` + `is_configured`/`SecretKeyUnavailable`）；`test_secrets.py` 覆盖往返/脱敏/fail-closed |
| T06 SQLite→PostgreSQL rehearsal + 原子切库 | **BLOCKED** | spec BLOCKED；生产 DSN 切换/切库前置未满足，不得提前 apply 后续版本 |

### 7.4 PH2 状态（Provider / 主题 / 回收站闭环）

| 任务 | 状态 | 说明 / 证据 |
|---|---|---|
| T01 原生 OpenAI/Anthropic/Gemini adapter + 受控 compatible profiles | **BLOCKED** | 需真实出网 + 受管 Provider 凭据；本地无凭据、无出网 |
| T02 Provider/Model canonical registry / capability / ownership / health / fallback / available-models | PARTIAL | 本地可做：registry 雏形 `get_runtime_chat_providers`（core/config.py:519）；**health / fallback 出网 BLOCKED**；available-models projection 经 qa/capabilities 已部分提供 |
| T03 个人中心 UI / Logo registry / selector fallback | **BLOCKED** | 前端工作 + 出网；无前端运行环境 |
| T04 语义 token / portal theme bridge / raw-color lint | **BLOCKED** | 前端工作；迁移主页面未做 |
| T05 trash deletion generation / Scheduler purge / Jobs Center cleanup projection | DONE-LOCAL | `services/retention.py`（`purge_expired_trash` + `schedule_purge_job`）+ `GET /jobs/cleanup` 投影端点（`9ee0e22`）；30 天 CHECK 为 PG-only，SQLite 测试走 `deletion_batch_id IS NULL` 路径 |

### 7.5 PH3–PH8 状态（生产前置门禁下方，全部 BLOCKED）

| Phase | 目标 | 状态 | 阻塞的生产前置 |
|---|---|---|---|
| PH3 知识库摄取/索引 | upload/版本/Chunk/Embedding/pgvector | **BLOCKED** | 对象存储、Embedding 服务、PostgreSQL+pgvector、`v4_006` apply 前置（PH1 切库） |
| PH4 AI Chat Engine | 多轮/分支/流式/上下文治理 | **BLOCKED** | Provider 出网+凭据（PH2-T01）、PG 切库（PH1-T06） |
| PH5 附件/OCR/Vision | 附件生命周期/Vision/OCR | **BLOCKED** | 对象存储、Vision/OCR 服务、出网 |
| PH6 KB+RAG | 检索/ACL/引用/拒答 | **BLOCKED** | pgvector、Embedding（PH3）、Provider（PH4） |
| PH7 UX/性能/回归 | 全路由/性能/对比/手册 | **BLOCKED** | 依赖 PH3–PH6 落地；性能/浏览器矩阵需生产环境 |
| PH8 生产发布验收 | 最终对账/发布/验收 | **BLOCKED** | 全部前置（出网/凭据/存储/队列/master key/备份演练/用户批准窗口） |

### 7.6 明确未做（禁止 mock）

- 未实现任何 Provider 真实 adapter、未配置任何受管凭据、未进行 PostgreSQL 生产切库、未部署对象存储/队列/worker、未做前端（T03/T04）、未跑 PH3–PH8 业务链路。
- 上述项在测试中不存在"伪通过"；任何涉及真实出网/生产数据的验收均标记为 `BLOCKED / NOT RUN`，不作为通过证据。

### 7.7 下一步建议（待用户/Sol 决策）

1. 满足 PH1-T02/T06 生产前置后，执行 SQLite→PG rehearsal 与原子切库（scheduler purge 依赖 PG 的 30 天 CHECK，生产侧才完整生效）。
2. PH2-T01/T02 health/fallback 需出网+凭据到位后由 Sol 门禁放行。
3. PH3 起需在对象存储/Embedding/pgvector 就绪后逐个 Phase 推进，每阶段独立 commit + 证据 + 用户批准窗口。

---

## 8. 续作记录 2（仓库盘点会话，2026-08-12 21:00 GMT+8）

> 本会话执行仓库盘点，未改业务代码。第 7 节之后另有会话已提交 PH3/PH4/PH5，本节如实记录提交、验证与工作区遗留状态。

### 8.1 分支与提交（截至盘点时刻）

- 分支：`codex/ekb-core-baseline-20260812`（`main` 无 CR 提交）
- 共 8 个提交，全部未 push（仅 `245a1ef` PH0 baseline 已在 origin）：

| Hash | 时间 | 内容 |
|---|---|---|
| `245a1ef` | 08-12 10:51 | chore(PH0): snapshot EKB core baseline |
| `3349505` | 08-12 17:56 | PH1+PH2 local slice：cutover ledger、retention governance、jobs API、secrets envelope、purge scheduler（11 文件，2733 行） |
| `9ee0e22` | 08-12 18:16 | PH2-T05 Jobs Center cleanup projection + PH1-T03 jobs 服务测试（3 文件，232 行） |
| `c9da146` | 08-12 18:17 | docs: 追加 PH1/PH2/PH3-PH8 诚实状态 |
| `e558b31` | 08-12 19:05 | **PH3**：storage/ingestion migration（`v4_006`）+ version pipeline、attempt state machine、upload API |
| `04141c1` | 08-12 19:58 | **PH4**：会话图服务（`v4_007`）、SSE v2 线缆契约、上下文预算、自动标题、chat_graph 路由；修复 AuthContext 无 user_id 导致的跨 PH 500（auth.user_id → auth.actor_id） |
| `3b26aa4` | 08-12 20:35 | **PH5**：附件服务/处理管线/视觉与 OCR 能力/路由（`v4_008`，入 v4_fullstack 链）+ 13 测试 |

### 8.2 当前验证状态（08-12 21:00 实测）

```bash
cd /Users/alin/EKB/apps/api && ../../.venv/bin/python -m pytest tests/v4 tests/v3 -q
# 结果：146 passed, 1 failed, 2 skipped（31s）
```

**失败 1 项**：`tests/v4/test_ph6_rag.py::test_citation_metadata_persisted_to_message`
- 根因：`store.py:1590-1592` 写入 `row.metadata_redacted`，但 `models.py` 的 `Message` 未定义该列/属性（`AttributeError: 'Message' object has no attribute 'metadata_redacted'`）。
- 该缺陷由**工作区未提交改动**（PH6 RAG 接线）引入，非已提交的 PH3-PH5 引入。

### 8.3 工作区未提交改动（重要遗留）

`git status` 显示大量未提交内容，**归属为另一条 PH6 RAG / fullstack rebuild 工作线，尚未提交**：

- **modified 74 文件，+7189 / -8599**：以 `apps/api` 为主（`qa.py` +1358、`llm.py` +730、`store.py` +477、`config.py` +304、`me.py` +288、`db.py` +273、`Dockerfile`、`pyproject.toml` 等），前端大量删除（`apps/web` 30 删 / 10 改 / 66 新）。
- **untracked 新文件（apps/api）**：
  - migrations：`v3_001~v3_007`、`v3_fullstack.py`、`v4_001_migration_provenance.py`、`v4_002_runtime_jobs.py`、`v4_003_provider_security.py`、`__init__.py`
  - services：`web_search.py`、`llm_provider_catalog.py`、`rag.py`、`turns.py`、`v3_analytics.py`、`v3_content_governance.py`、`v3_identity.py`、`v3_llm.py`、`v3_profile.py`、`v3_trash.py`
  - routers：`analytics.py`、`content_governance.py`、`identity_v3.py`、`llm.py`、`trash.py`
  - 其他：`ensure_admin.py`
- **untracked 测试**：`tests/v4/test_ph6_rag.py`（PH6 RAG 测试，其中 1 项当前失败）。

### 8.4 重大发现：v3/v4 migration 未纳入版本控制

- `git ls-files apps/api/ekb_api/migrations/` 仅 6 个文件（`v4_004`~`v4_008` + `v4_fullstack.py`）。
- 磁盘上共 14 个 migration 文件；**`v3_001~v3_007`、`v3_fullstack.py`、`v4_001/2/3` 从未被任何提交跟踪**（含 main 分支），仅存在于工作区。
- 风险：这些是迁移链的核心代码（v3 全栈合同 + PH1 provenance/runtime/provider-security），一旦 `git clean`/切换分支即可能丢失；PH3-PH5 提交的 evidence 虽验证了 `v4_001→v4_002`，但文件本身不在版本控制中。
- `v4_005_retention_governance.py` 有 14 行未提交追加（内容待 diff 确认）。

### 8.5 下一步建议（优先级排序）

1. **修复 PH6 测试失败**：`models.py` 补 `metadata_redacted` 字段或改 `store.py:1590-1592` 读取逻辑，使 `test_ph6_rag.py` 全绿。
2. **决定未提交改动的归属**：确认 PH6 RAG / fullstack rebuild 工作线是否继续；继续则按 PH6 门禁（pytest+ruff 全绿）后独立提交，否则清理或 stash。
3. **把未跟踪 migration 纳入版本控制**：将 `v3_*`、`v4_001/2/3` add 进分支并提交，消除迁移链丢失风险。
4. PH6 提交后再更新本节状态表，并按原 7.7 建议推进生产前置项。

## 9. 续作记录 3（本地真实业务闭环，2026-08-13）

本会话继续既有工作区实现，优先完成本地可见的真实业务链路，没有执行服务器写入。详细证据见 `docs/evidence/ekb-core-rebuild/local/2026-08-13-local-business.md`。

### 9.1 已完成的本地切片

- LLM-only：聊天与 Embedding 只读取远程受管 Provider/Model；缺少远程 Embedding 时稳定返回 `EMBEDDING_UNAVAILABLE`，不回退本地模型、不写伪向量。
- 上传：真实 batch → upload session → 对象 PUT → checksum complete → ingest job → worker 读取/解析/分块/Embedding/索引或失败状态更新。
- 对象存储：本地显式配置的磁盘适配器可真实写入；S3-compatible presigned PUT 实现不向远程 URL 携带 EKB Bearer header；生产对象存储仍未执行。
- 浏览器：真实 Chrome 登录、文档中心、批量上传和刷新已验收；页面真实显示服务端文档及 `已就绪`/`失败 · EMBEDDING_UNAVAILABLE` 状态，首次加载态缺口已修复。

### 9.2 本地验证

- 后端：`333 passed, 2 skipped`。
- 前端：typecheck、49 tests、production build 全部通过。
- npm 生产依赖审计（官方 registry）：`found 0 vulnerabilities`。
- 本次修改范围后端 `ruff --ignore E501,F401`、compileall 和 `git diff --check`：通过；触及的旧文件仍有历史 E501/F401 风格债务，未扩大修复范围。
- Playwright 真实浏览器：最新附件 session/PUT/process/ask 全部 200；阶段完成、seq=22、回答准确读取附件并展示附件引用。首次暴露的 thinking-level 400 已修复后复验通过。
- 图片路径已收紧为远程 LLM-only：远程模型 capability 支持 Vision 时发送 typed image content；无远程 Vision/OCR 时处理失败，不写本地占位文本；S3 session 返回 presigned PUT。

### 9.3 仍未完成

生产 PostgreSQL/pgvector 原子切库、生产对象存储、可靠队列/Worker/Scheduler、外部 Provider/Embedding 凭据、服务器备份/部署/回滚和生产浏览器验收仍未执行；未满足这些前置时不得把 PH1–PH8 标成生产完成，也不使用 mock 绕过。
- 服务器入口实际检查：`bash scripts/deploy/deploy.sh --skip-tests` 因缺少运行时 `DEPLOY_HOST` 以退出码 64 fail closed；没有 SSH/备份/迁移/重启/线上写入。

## 12. CR-PH3-T06 Upload Center 本地切片交付记录（2026-08-13）

- DONE-LOCAL（仅本地切片）：Upload Center 真实服务端 batch projection 已覆盖 canonical `queued`、`uploading`、`verifying`、`processing`、`indexing`、`ready`、`failed`、`cancelled`；projection 从持久化 batch/session/item/ingest job 读取 counts、阶段/进度、attempt、job id 和脱敏 error code/message/retryable 信息。未知、越权或非当前租户 batch/item fail closed。
- 刷新恢复：前端用 `ekb.upload-center.batch-refs.v1` 保存已创建 batch refs，页面启动和刷新后逐个重新 GET 服务端 projection；不依赖内存状态。failed 且 retryable item 使用真实 `retryUploadJob`，processing/uploading 使用真实 cancel/abort；操作后重新 GET，未写假成功。
- 浏览器实测：此前本地真实 Chrome 已验证文档中心的上传/刷新路径可见服务端状态；本地 embedding provider 缺失时真实 worker 失败码为 `EMBEDDING_UNAVAILABLE`，页面显示失败而不是伪造 ready。此条只记录本地浏览器证据，不扩展为生产验收。
- 验证：后端 `apps/api/tests/v4/test_ph3_upload_center.py`、`test_ph3_ingestion.py`、`test_ph3_boundaries.py` 定向回归 `34 passed`；前端 `52 passed`，typecheck/build 通过；`git diff --check` 和本次文档 diff secret scan 通过。
- NOT RUN/BLOCKED：生产 PostgreSQL/pgvector、对象存储、可靠队列/Worker/Scheduler、服务器备份、部署/回滚和生产浏览器均未执行，原因是当前运行时没有生产目标/受管凭据。没有写入或保存任何凭据、私网地址或 token；本地 SQLite/开发对象存储不代表生产完成。
- 下一步本地缺口：需要受管远程 embedding/provider 与生产基础设施配置后验证成功索引及 PG/pgvector、对象存储、worker/scheduler；当前 Upload Center 恢复的是已知 batch refs，不是跨设备的服务端全局批次列表。CR-PH3 其余能力及 PH4–PH8 仍未整体完成。

## 10. 续作记录 4（本地 DONE-LOCAL，2026-08-13）

- 真实 `POST /api/v1/attachments/{attachment_id}/promotions` 已完成本地验证：租户/owner/live ACL、路径冲突、回收站保留、幂等、并发处理；复用 attachment `source_object_id` 创建 document/version/job，返回 `202 QUEUED`，worker 状态投影一致。
- 前端助手提升入口已补齐；LLM-only 主题支持 `light/dark/auto` semantic tokens。
- 验证：backend `343 passed, 2 skipped`；web `51 passed`、typecheck/build；ruff/compileall/diff check；真实浏览器登录后 API `202` 且数据库 `source_object_id` 一致。
- `NOT RUN/BLOCKED`：生产 PostgreSQL/对象存储/队列、服务器备份部署重启、生产浏览器，因无运行时 `DEPLOY_HOST`/受管凭据未执行。不要把本地 SQLite 或 `QUEUED` 写成生产完成。`git diff --check`。

## 11. CR-PH4-T07 最小契约收尾（本地，2026-08-13）

- DONE-LOCAL：联网 Web Search 已从 QA UI/API/runtime/config 移除；QA 只使用当前授权 KB RAG、附件上下文和远程 LLM Provider/Model。
- 兼容边界：旧请求 `options.web_search` 在 retrieval 前明确返回 `400 FEATURE_REMOVED`，不会触发 retrieval 或任何网络搜索调用；KB search 与 Strict refusal 保持独立。
- Composer 真实浏览器状态不再显示联网搜索；添加文件、远程模型选择和 KB 上下文仍可见。
- 验证记录：backend `345 passed, 2 skipped`；web `51 passed`、typecheck/build 通过；本次定向 API `28 passed`，RAG/多轮回归 `21 passed`，`git diff --check` 通过。
- 生产 PostgreSQL/pgvector、对象存储、队列/Worker/Scheduler、服务器备份/部署/回滚和生产浏览器仍未执行；本地证据不代表生产完成。
## 2026-08-13 Promotion and theme local slice

- DONE-LOCAL：真实 POST /api/v1/attachments/{attachment_id}/promotions；校验 tenant/owner/live KB ACL、路径冲突/回收站保留/幂等/并发；复用 attachment source_object_id 创建 document/version/ingest job，返回 HTTP 202、状态 QUEUED，worker 投影真实状态，不把队列写成成功。
- DONE-LOCAL：助手附件“提升”入口和真实错误/状态/ID 显示；主题支持 semantic tokens、light/dark/auto、系统主题和 storage 同步、reduced-motion；LLM-only，不使用本地模型/mock。
- 验证：后端 343 passed, 2 skipped；前端 51 passed、typecheck/build 通过；ruff、compileall、git diff --check 通过；真实浏览器登录后实调 KB 与 promotion API 返回 202，数据库确认 document_version.source_object_id 与 attachment 一致。
- NOT RUN/BLOCKED：生产 PostgreSQL/pgvector、对象存储、可靠队列/Worker/Scheduler、服务器备份/部署/重启/生产浏览器；运行时 DEPLOY_HOST 和受管凭据未配置，不猜测、不输出、不落盘。生产未完成，不能标记 PH0–PH8 全部完成。

## 2026-08-13 Provider/Model 联动本地切片

- DONE-LOCAL：`GET /api/v1/qa/capabilities` 与显式 `POST /api/v1/qa/ask` model 选择共享当前 `tenant+actor` 的活动加密 credential 与已启用远程 Provider/Model registry；模型 capability/vision 信息直接来自 DB registry，不构造默认模型或本地模型。
- DONE-LOCAL：unknown、disabled、cross-tenant、无活动 credential 的显式 model 返回稳定 `MODEL_UNAVAILABLE`；图片请求选择非 `vision=true` 模型返回 `MODEL_NOT_ALLOWED`；显式选择固定对应 provider，不静默 fallback；未指定 model 的普通请求保留既有 provider fallback。
- 验证：后端 `349 passed, 2 skipped`；前端 `51 passed`、typecheck/build 通过；Provider/Model 定向回归 `63 passed`；范围 Ruff（忽略既有 E501 等风格债务）、compileall、`git diff --check` 通过。新增测试的全局 breaker、Provider、credential 状态已 teardown 隔离。
- 浏览器：此前真实浏览器已确认 Profile 模型服务目录、Assistant 添加文件、模型选择和 KB context 可见；本切片未以浏览器或外网调用伪造 Provider 成功。
- NOT RUN/BLOCKED：真实 Provider 出网、生产 PostgreSQL/pgvector、生产对象存储、队列/Worker/Scheduler、服务器备份/部署/回滚和生产浏览器验收仍未执行；无凭据写入文档。未满足生产前置时不能标记 PH0–PH8 全部完成。

## 2026-08-13 本地 Embedding 切片交付记录

- DONE-LOCAL：`services/embedding.py` 使用真实 OpenAI-compatible 远程客户端；worker 绑定 `tenant+actor+KB ACTIVE profile/generation`。无配置或无 ACTIVE profile/generation 时返回 `EMBEDDING_UNAVAILABLE` 并 fail-closed，不写 READY。
- 远程响应校验 `count`、向量 dimension 和非有限值；无本地模型或伪向量。真实外部 Provider 未调用，不宣称远程出网成功。
- 验证：Embedding 定向 `40 passed`；全量后端 `358 passed, 2 skipped`；Ruff、compileall、`git diff --check` 通过。生产 Provider 出网/凭据、PostgreSQL/pgvector、部署仍 `NOT RUN/BLOCKED`；不代表全 PH3/PH8 或 PH0–PH8 完成。

## 2026-08-13 CR-PH3-T06 Upload Center 跨设备批次列表收尾

- DONE-LOCAL（增量收尾）：新增租户作用域 `GET /api/v1/kb/uploads/batches`；服务端对 `limit` 实施 `1–100` 有界约束（默认 `30`），Upload Center 列表以服务端 batch projection 为权威来源。`localStorage` 键 `ekb.upload-center.batch-refs.v1` 仅用于 fallback/reconciliation，不作为跨设备列表真值。本条更新前一条“仅恢复已知 batch refs、尚非跨设备服务端全局列表”的本地状态；此前条目保留为历史记录。
- 本地浏览器证据（2026-08-13）：使用现有本地开发账号重新建立登录后，删除 `ekb.upload-center.batch-refs.v1` 并 reload `/#/documents`，真实服务端 batch 被恢复并显示；真实文件为 `HANDOFF-2026-08-12.md`，失败阶段为 `CHUNKING / 50%`，attempt `1/1`，页面显示 job id 和 `EMBEDDING_UNAVAILABLE`。不记录 access/refresh token、密码、私有地址或 opaque secret。
- 验证：backend full pytest `361 passed, 2 skipped`；web `52 passed`；web typecheck/build 通过。
- NOT RUN/BLOCKED：生产/服务器工作仍未执行；当前运行时未提供生产目标（统一写作 `[production host]`）或受管凭据（统一写作 `[REDACTED]`）。不宣称生产部署，不宣称全 PH3 或 PH8 完成。

## 13. CR-PH2-T05 / FR-058 Jobs Center 本地 UI 与真实浏览器证据（2026-08-13）

### 13.1 DONE-LOCAL 前端切片

- 现有 Analytics `GovernancePanel` 已增加真实「作业中心」Tab。数据来自当前租户作用域的 `GET /api/v1/jobs`、`GET /api/v1/jobs/cleanup`，取消调用 `POST /api/v1/jobs/{job_id}/cancel`。
- 页面以服务端返回为准展示 state counts、job type、safe timestamps、lease/heartbeat availability、脱敏 error metadata、job id，以及 cleanup 的 `total`、`by_state`、`recent`。payload、`tenant_id`、Provider 原始响应和私有地址均排除在 UI 和证据之外。
- 只有 `QUEUED`、`RUNNING`、`RETRY_WAIT` 状态提供取消；取消后重新加载服务端状态，不写入本地假成功。

### 13.2 2026-08-13 本地真实浏览器证据

- 使用现有本地开发账号完成认证后，进入 `Analytics → 运营与治理 → 作业中心`，页面展示 5 条真实作业：4 条 `SUCCEEDED`、1 条 `DEAD`；其中包含一条文档 ingest 作业，错误状态为 `EMBEDDING_UNAVAILABLE`。
- cleanup projection 展示 `retention_purge` 总数 4，4 条均为 `SUCCEEDED`。缺少 lease/heartbeat 时页面明确显示 unavailable。
- 认证过期阶段在该浏览器会话中已有 3 条较早的 auth-related console errors，因此不记录或声称 console=0；Jobs Center 自身的数据加载成功。页面显示 job id，但本文不记录完整 opaque ID、凭据、token 或私有地址。

### 13.3 验证与边界

- Web：`57 passed in 8 files`；typecheck/build 通过；`git diff --check` 通过。
- 后端未改动，使用既有全量结果 `361 passed, 2 skipped` 作为背景；本次 Jobs Center UI 切片未重新运行后端测试，不将其写成新的后端回归。
- 生产目标、服务器备份、部署和回滚均 `NOT RUN/BLOCKED`：当前没有生产目标或受管凭据，文档中统一使用 `[production host]`、`[REDACTED]`。本地结果继续保持 LLM-only、无本地模型、无 mock 边界；不宣称 PH2 或 PH0–PH8 全部完成。

## 14. Dashboard semantic-theme 本地切片（2026-08-13）

### 14.1 DONE-LOCAL 范围

- 仅完成 Dashboard route 的主题切片：`apps/web/src/app-v2/pages/DashboardPage.tsx` 中全部 31 个 page-level raw `#hex`/`rgb`/`rgba` 颜色字面量，已替换为 tokens/theme 中已有的 semantic variables。
- 未改变 backend、API 或 data behavior；未引入 fake data。LLM-only、无本地模型、无 mock 边界保持不变。
- 新增 source contract test：`apps/web/src/app-v2/tests/v3.dashboard-theme.test.ts`，断言 `DashboardPage` 不包含 raw color literals。

### 14.2 2026-08-13 本地真实浏览器证据

- 认证后的本地浏览器打开 `/#/dashboard`，Dashboard 显示真实 workspace metrics/content：4 个 documents、3 个 knowledge spaces、trends、recent activity 和 governance proxy states。
- 在当前认证会话的 localStorage 设置 `v2.theme=dark` 后 reload；Dashboard 仍以真实数据渲染，且主题状态已应用。
- 当前浏览器会话累计已有 13 条较早的 auth-related console errors，因此不宣称 `console=0` 或 full route matrix；未观察到新的 Dashboard data failure，也不记录 visual contrast score。本文不记录 token、password 或 private address。

### 14.3 验证与边界

- Web：`58 passed in 9 files`；typecheck/build 通过；`git diff --check` 通过。
- Backend 未改动；本切片不宣称新的 backend suite 结果。
- 生产目标、服务器 backup、deploy、rollback 均 `NOT RUN/BLOCKED`，原因是没有 production target/managed credentials；统一使用 `[production host]` 和 `[REDACTED]`。
- 本条只代表 Dashboard route migration，不宣称 `FR-001–006`、PH2 或 PH0–PH8 全部完成。

## 15. 2026-08-13 Office parser 本地证据

- DONE-LOCAL：已推送 commit `5a008f17251fa8e37ee8f44980afd923055a8957` 在 `services/parsers/registry.py` 增加真实 XLSX/PPTX parser，使用 `openpyxl` / `python-pptx`；字节级测试覆盖正常解析，以及空、损坏、旧 Office 输入的 fail-closed。详细证据见 `docs/evidence/ekb-core-rebuild/local/2026-08-13-office-parser.md`。
- XLSX/PPTX 解析保持 LLM-only 边界：不引入本地大模型、mock、伪向量或伪成功；XLSX 只读且不执行宏，解析输出保留 workbook/sheet/row/cell 结构，PPTX 保留 slide/shape/notes（可读时）结构。
- 验证：定向 `38 passed`；后端全量 `370 passed, 2 skipped`；前端 `58 passed`；typecheck/build 通过。
- 以上全量数字是当前工作树回归快照，不声称全部由本 parser 切片单独造成。
- 浏览器只验收当前 Documents、Assistant、Jobs Center 的真实数据；不声称浏览器成功摄取 XLSX/PPTX。
- `NOT RUN/BLOCKED`：生产 PostgreSQL/pgvector、Provider/Embedding 出网、对象存储、可靠队列/Worker/Scheduler、服务器备份/部署/回滚仍未执行。生产目标统一写作 `[production host]`，受管敏感值统一写作 `[REDACTED]`；不记录凭据、token 或私密地址。

## 2026-08-13 browser OOXML validation

- 后续本地真实浏览器验证：在 `Documents → 批量/目录上传 → 批量多文件` 中选择 `/tmp/ekb-parser-qa.xlsx` 和 `/tmp/ekb-parser-qa.pptx`；两份仅为临时文件，不进 Git。
- UI 扫描识别为 Excel/PPT，各 1 个；真实上传完成 `2/2`；服务端分别创建真实 batch/job/document 状态。
- worker 处理后两个条目均到达 `CHUNKING · 50%`；解析器未报 `MIME_UNSUPPORTED` 或 `PARSER_CORRUPT`。由于本地没有远程 Embedding provider/profile，真实终态为 `EMBEDDING_UNAVAILABLE/FAILED`；不得写成 `READY`、成功索引或远程 Provider 成功。
- 浏览器仍有历史 auth-related console errors；不声称 `console=0`。
- 生产 PostgreSQL/pgvector、Provider 出网、对象存储、可靠队列/Worker/Scheduler、服务器备份/部署/回滚仍为 `NOT RUN/BLOCKED`。生产目标统一使用 `[production host]`，受管敏感值统一使用 `[REDACTED]`；不记录 token、password 或私密地址。

## 16. 2026-08-13 Local scheduler interval 与 Office 上传真实失败闭环

### 16.1 DONE-LOCAL 范围

- 已推送 commit `524e51a`：`apps/api/ekb_api/main.py` 新增 `EKB_LOCAL_SCHEDULER_INTERVAL` 的 `1–300` 秒有界解析；非法、越界或非十进制输入回退 `60` 秒，仅在 development 本地 scheduler 分支读取并生效。
- 新增 `apps/api/tests/test_local_scheduler.py`，覆盖本地 scheduler interval 解析/调度边界。
- 本地 API 用 `interval=2` 启动后，真实 XLSX/PPTX 上传批次由后台 tick 从处理中推进到 `CHUNKING · 50%`，随后真实失败为 `EMBEDDING_UNAVAILABLE/FAILED`；未出现 `MIME_UNSUPPORTED`、`PARSER_CORRUPT` 或伪 `READY`。

### 16.2 验证记录

- local scheduler + PH3 office/ingestion/scheduler 定向回归：`34 passed`。
- Ruff 与代码 diff check：通过。
- 本次文档更新的 `git diff --check` 与新增文档 secret scan：通过。
- 详细本地证据：[`docs/evidence/ekb-core-rebuild/local/2026-08-13-local-scheduler.md`](evidence/ekb-core-rebuild/local/2026-08-13-local-scheduler.md)。

### 16.3 生产边界

- 以下仍为 `NOT RUN/BLOCKED`：生产 PostgreSQL/pgvector、Provider/Embedding 出网、生产对象存储、可靠队列、生产 Worker/Scheduler、服务器备份、部署与回滚。
- 生产目标统一写作 `[production host]`，受管敏感值统一写作 `[REDACTED]`；本文及新增证据不记录凭据、token 或私密地址。
- 本条只证明本地 development scheduler 与真实 Office 上传失败路径，不代表生产 scheduler、成功索引或 PH3/PH8 完成；既有历史记录保留。
## 17. 2026-08-13 回收站 restore / purge 本地闭环

### 17.1 DONE-LOCAL 范围

- Luna 修改 `apps/api/ekb_api/services/v3_trash.py`，补齐 restore 的保留期、purge-state、CAS 和 tenant scope 防护；purge 增加 expiry/state/generation 防护；同一资源重复删除时创建新的 deletion generation 和 retention window。
- 新增 `apps/api/tests/v4/test_ph2_trash.py`，覆盖恢复、过期、purging、purge、跨租户和重复删除分代等真实 SQLite 集成场景。
- Document 删除历史状态缺失时，restore 只能安全 fallback 为 `READY`；不能把该 fallback 写成历史状态完全恢复。对象/向量分阶段清理由其他 worker/service 负责，本切片不越界宣称完成。

### 17.2 本地验证与浏览器证据

- 专项：`6 passed`。
- 后端全量：`385 passed, 2 skipped`。
- 指定文件 Ruff：`All checks passed!`；`git diff --check`：通过。
- 真实浏览器（`127.0.0.1:5173`）：在 `/#/knowledge` 创建“回收站本地验收-20260813”，删除后在 `/#/recycle` 看到真实条目和 30 天保留；点击“还原”显示“已还原…”；知识库重新出现在当前账号授权列表。浏览器 console 有既存错误，不声称 `console=0`。
- 详细命令、结果、时间和边界见 [`docs/evidence/ekb-core-rebuild/local/2026-08-13-recycle-restore.md`](evidence/ekb-core-rebuild/local/2026-08-13-recycle-restore.md)。

### 17.3 Production boundary / next step

- `NOT RUN`：生产 PostgreSQL、对象存储、可靠队列、Worker/Scheduler、Provider 凭据和外部出网、服务器备份、部署、回滚及生产浏览器均未运行。统一使用 `[production host]` / `[REDACTED]`，不记录凭据、token 或私有地址。
- 本地 SQLite 与浏览器结果不代表生产回收站闭环，也不代表 PH0–PH8 全部完成。后续应在受管生产目标可用后做 DB/object/index 分阶段清理的备份、回滚和一致性验收，并为新 DOCUMENT 删除入口持久化删除前状态。

## 2026-08-13 文档中心批量删除本地切片

### DONE-LOCAL

- 修改文件：`apps/web/src/app-v2/pages/DocumentsPage.tsx`、`apps/web/src/app-v2/components/documents/DocumentTable.tsx`、`apps/web/src/app-v2/adapters/documents.ts`、`apps/web/src/app-v2/tests/m3.test.ts`。
- 文档中心支持真实行/表头选择、当前可见页全选、选中数展示和二次确认。确认后逐条调用已有真实单文档 `DELETE`；成功项进入既有回收站，失败项保留并显示部分失败结果。
- 删除完成后重新读取服务端文档列表并清理已不存在的 stale selection；能力投影 `documents.bulk-delete=available`。该切片不是新增“真实批量删除 API”，而是对既有真实单文档 DELETE 的逐条批处理。

### 本地浏览器证据

- 在 `127.0.0.1:5173/#/documents` 的真实服务端文档列表中，选择当前可见文档后显示“已选择 6 项”，按钮显示为已启用的“批量删除 (6)”；本次未点击删除，未误删本地真实数据。
- 浏览器会话仍有既存 console 错误，因此不能写作或声称 `console=0`。

### 验证与生产边界

- `npm test -- --run`：`58 passed`；typecheck：passed；build：passed（保留已有 chunk `>500KB` warning）；`git diff --check`：passed。
- 本地证据不扩展为生产完成。生产 PostgreSQL/对象存储/队列/Worker/Provider、服务器备份、部署和回滚仍 `NOT RUN`；使用 `[production host]` / `[REDACTED]`，不记录凭据、token 或私有地址。
- 详细证据：[`docs/evidence/ekb-core-rebuild/local/2026-08-13-document-bulk-delete.md`](evidence/ekb-core-rebuild/local/2026-08-13-document-bulk-delete.md)。

## 2026-08-14 Jobs runtime 与 legacy Office 本地收口

### DONE-LOCAL

- Jobs runtime：后端新增真实 runtime projection，返回 tenant-safe 的 worker heartbeat、retention lease 和 scheduler runs；Analytics 作业中心前端真实调用并展示 `available` / `unavailable`，不暴露 payload、counters、tenant 或错误详情。
- Legacy Office：FR-015 的 DOC/XLS/PPT legacy MIME 已接入隔离的 `soffice` 转换链，复用现有 OOXML parser；转换使用 `shell=False`、独立临时目录/profile 和 30 秒硬超时，目标 parser 失败仍透传原有错误分类。

### 本地验证与浏览器证据

- 后端全量：`390 passed, 2 skipped`；Web：`60 passed`；typecheck/build：通过；legacy Office 定向：`14 passed`；相关 Ruff：通过；`git diff --check`：通过。
- 真实本地 smoke 已执行 XLS → XLSX → `XlsxParser` 的真实 `soffice` 转换链。该 smoke 不扩展为 DOC/PPT 已真实成功解析的声明。
- 本地认证后的 Analytics 作业中心实际显示真实 worker heartbeat、retention lease 和最近 `SUCCEEDED` scheduler run。API 重启前旧进程对新 runtime endpoint 返回过 404，重启后返回 200。浏览器仍有历史 auth-related console errors，不能写作或声称 `console=0`。
- 详细证据：[`2026-08-14-jobs-runtime.md`](evidence/ekb-core-rebuild/local/2026-08-14-jobs-runtime.md) 与 [`2026-08-14-legacy-office.md`](evidence/ekb-core-rebuild/local/2026-08-14-legacy-office.md)。

### Production boundary / next step

- `NOT RUN/BLOCKED`：生产 PostgreSQL/pgvector、远程 Provider/Embedding 出网、对象存储、可靠队列/生产 Worker、服务器备份、部署和回滚仍未执行；生产目标统一写作 `[production host]`，受管敏感值统一写作 `[REDACTED]`。
- 本条仅记录 CR-PH2 runtime/jobs 与 CR-PH3 legacy parser 的 `DONE-LOCAL` 切片，不代表相关 Phase exit gate 或 PH0–PH8 全部完成。

### 2026-08-14 runtime stale heartbeat 修复

- scheduler 默认 `owner_id` 改用稳定进程标识；runtime projection 对 `local-scheduler` 仅展示仍存在于 `scheduler_leases.owner_id` 的当前 worker。历史替换 worker heartbeat 保留，但不再作为当前 worker展示。
- 测试、Ruff 与 `git diff --check` 通过；浏览器复验显示 `1 worker / 1 lease / 真实 SUCCEEDED run`。
- 生产仍未执行，生产目标使用 `[production host]`，受管敏感值使用 `[REDACTED]`；不记录凭据、token、私网地址或完整 opaque ID。

## 2026-08-14 TeamPage 成员邀请与目录导出本地闭环

### DONE-LOCAL

- `apps/web/src/app-v2/pages/TeamPage.tsx` 已启用真实邀请 Modal：使用 `services.admin.inviteUser`，提交期间禁用，成功清空密码并重新调用 `services.identity.listUsers`；失败只显示脱敏 adapter message、code 和 request id。
- `apps/web/src/app-v2/pages/teamDirectory.ts` 提供邀请字段/角色校验、权限按钮状态和真实已加载成员 CSV；CSV 使用 RFC4180 quoting，并为 `= + - @` 开头值加单引号前缀，固定文件名不含密码、tenant 或 token。
- `/api/v1/admin/users` 已从 `CAP_KB_WRITE` 收紧为 `team:user:manage`；旧签发的 OWNER/ADMIN legacy token 通过角色兼容分支可用，普通 MEMBER 被拒绝并写入无密码审计记录。
- `SqlStore.create_user` 在无 v3 表或租户角色缺失时保留 legacy user 创建成功；缺少 v3 registry 不视为失败。v3 表和角色完整时，才在同一事务内同步 `tenant_memberships` 与 `user_profiles`，使新建本地账户在真实 v3 目录刷新后可见。

### 验证与边界

- 兼容性测试 `../../.venv/bin/pytest -q tests/v3/test_identity_users.py::test_existing_admin_user_create_path_remains_registered`：`1 passed`；无 v3 role registry 时只断言 legacy user 创建成功，有 registry 时才断言 v3 directory 可见。
- identity/users 定向命令 `../../.venv/bin/pytest -q tests/v3/test_identity_users.py`：`8 passed`，覆盖 OWNER/ADMIN 邀请、MEMBER 拒绝、输入边界、审计无密码和 v3 投影可见性。
- 修复前的串行全量后端快照为 `397 passed, 1 failed, 2 skipped`，唯一失败为上述兼容性测试；按本次范围未重跑全量。
- 前端新增 `apps/web/src/app-v2/tests/v3.team-directory.test.ts`，定向 Vitest `4 passed`，覆盖邀请 adapter 成功/失败、按钮状态、字段校验及 CSV quoting/公式注入防护；文件名符合现有 Vitest include。
- 仅记录本地成员账户创建与 CSV 导出，不宣称邮件投递、生产身份平台、角色 CRUD、SCIM、生产部署或 PH0–PH8 完成。服务器、备份、部署和回滚仍 `NOT RUN/BLOCKED`；生产统一使用 `[production host]` / `[REDACTED]`，不记录密码、token 或私有地址。

## 2026-08-14 Assistant 会话导出本地收尾

- DONE-LOCAL：`canExportConversation` 仅在 ready、非流式且存在非 transient 消息时启用；Markdown 只导出当前服务端已持久化的消息，过滤流式 transient 内容。
- Share 保持 disabled；本切片不新增分享链接、公开访问或服务端导出存储。
- 仅运行会话导出定向测试与 `git diff --check`；本地证据见 [`docs/evidence/ekb-core-rebuild/local/2026-08-14-conversation-export.md`](evidence/ekb-core-rebuild/local/2026-08-14-conversation-export.md)。生产、部署、备份、回滚和 PH0–PH8 全部完成仍 `NOT RUN/BLOCKED`。
- 不记录密码、token、API key 或私密地址；生产统一使用 `[production host]` / `[REDACTED]`。

## 2026-08-14 会话重命名本地切片

- DONE-LOCAL：后端已有 `PATCH /api/v1/chat/conversations/{id}` 已由前端真实接入，覆盖 `ApiClient`、conversation adapter、受控 UI 操作和成功后的服务端会话刷新；未知客户端异常不泄漏原始 `message`。
- 验证：`npx vitest run src/app-v2/tests/v3.conversation-rename.test.ts` 为 `2 passed`；`npm run test` 为 `12 files / 68 tests passed`；`npm run typecheck` passed；`npm run build` passed，并保留既有 `>500KB` chunk warning。
- Share 和 Projects 仍保持 disabled。本条只代表本地真实会话重命名，不代表 PH0–PH8 完成；服务器、备份、部署和回滚仍为 `NOT RUN/BLOCKED`，生产统一使用 `[production host]` / `[REDACTED]`。

## 2026-08-14 会话分支本地切片

- DONE-LOCAL：真实接入 `GET /chat/conversations/{id}/branches`、`POST /chat/conversations/{id}/active-branch` 和 `GET /chat/conversations/{id}/messages?branch_id=`；`ApiClient` 兼容旧数组与新 `{messages}` 响应，adapter 负责分支映射和安全错误，`AssistantPage` 使用真实选择器并在分支变更后刷新消息。
- 验证：`npx vitest run src/app-v2/tests/v3.conversation-branches.test.ts` 为 `4 passed`；`npm run test` 为 `13 files / 72 tests passed`；`npm run typecheck`、`npm run build`、`git diff --check` 通过。
- 当前浏览器未认证；Share/Projects 保持 disabled。
- 本地切片不能宣称 PH0-PH8 或生产完成；生产/服务器、备份、部署和回滚仍 `NOT RUN/BLOCKED`，不记录秘密。
- 详细证据见 [`docs/evidence/ekb-core-rebuild/local/2026-08-14-conversation-branches.md`](evidence/ekb-core-rebuild/local/2026-08-14-conversation-branches.md)。

## 2026-08-14 legacy store.save_message 到 chat branch graph 本地桥接

- DONE-LOCAL：在 v4 schema 下，`store.save_message` 为 legacy QA 消息自动创建/激活 root branch，并写入 `branch_id`、`parent_message_id`、`content_hash`；无 v4 列时保持 legacy 路径。
- 验证：`bridge-check` 临时 SQLite 为 `PASS`；PH4 `32 passed`；API `28 passed`；ruff/compileall `passed`。
- `qa.py` 的多轮 history graph bridge 尚未完成；当前浏览器未认证。本条仅为本地开发切片，不宣称 PH4 或 PH0–PH8 全部完成。
- 生产/服务器（`[production host]`）、备份、部署、回滚均 `NOT RUN/BLOCKED`；不记录密码、token、API key 或私密地址。证据见 [`docs/evidence/ekb-core-rebuild/local/2026-08-14-chat-branch-bridge.md`](evidence/ekb-core-rebuild/local/2026-08-14-chat-branch-bridge.md)。
